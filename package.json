{
  "name": "pdfmasterpro",
  "version": "1.0.0",
  "description": "Advanced PDF processing web application - Merge, split, compress, convert, and edit PDFs",
  "scripts": {
    "install:all": "cd server && npm install && cd ../client && npm install",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm install && npm run build",
    "start": "cd server && npm start"
  },
  "keywords": ["pdf", "merge", "split", "compress", "convert"],
  "license": "MIT"
}
